# Free Gemini key (no card): https://aistudio.google.com/apikey  — used for extraction + fallback
GEMINI_API_KEY=your_gemini_key_here
# Optional free OpenRouter key (no card): https://openrouter.ai/keys — used for the rewrite (DeepSeek)
OPENROUTER_API_KEY=your_openrouter_key_here
